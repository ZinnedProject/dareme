<%= link_to image_tag(event.image_url(:thumb_200)), event %> <br />
<%= link_to event.title, event %> 
<%= link_to 'Stop Following', Following.find_by_followable_type_and_followable_id_and_user_id("Event",event.id,current_user.id), method: :delete, data: { confirm: 'Are you sure?' } %> 